# Test log

Generated by `node scripts/test-log.mjs`. **Do not edit by hand** — a count typed manually is wrong as soon as the next test lands, and a stale number here would look authoritative while being false.

- Generated: `2026-09-03T06:39:31.486Z`
- Host platform: `win32`
- Toolchain: `rustc 1.98.0 (88d9e12ae 2026-08-18)`
- Overall: **all checks passed**

**Counts are host-dependent.** `recorder/src/strace.rs` tests are `cfg(target_os = "linux")`, the E2E suite is Linux-only, and the aya-backend set cannot be verified on Windows at all. A Linux run of this commit therefore reports more tests than a Windows run — more of the suite exists there. The committed copy of this file is generated on Linux, which is the platform the product runs on.

## What is NOT covered

Leading with this because a bare pass count would misrepresent the state of the project. Platform-specific testing boundaries and their CI verification status:

| Area | Local Tests | CI Verification |
|---|---|---|
| `recorder/aya-ebpf/` | Nightly target | Compiled and verifier-checked in `phase2-aya.yml` on standard Linux runners. |
| `recorder/src/aya.rs` load/attach/drain | Unit tests in `cargo test` | Load/attach/drain and parity harness executed in `phase2-aya.yml`. |
| `recorder/src/strace.rs` `record()` | 11 `#[ignore]`d | Needs Linux with `strace`. Run by `phase1-e2e.yml` and `g2-strace-harness.yml`, not by local `cargo test`. |
| `harness/g1/` | Gate tooling | Verified by the G1 workflow run on Linux runners, not by local unit tests. |

Host-specific kernel probes and strace execution are continuously verified by their respective GitHub Actions workflows.

## Rust tests

| Feature set | Passed | Failed | Ignored | clippy `-D warnings` |
|---|---|---|---|---|
| `default` | 555 | 0 | 0 | clean |
| `aya-backend` | _skipped_ | _skipped_ | _skipped_ | _skipped_ |

Both feature sets are listed because the aya backend is optional: a warning in `recorder/src/aya.rs` is invisible to a default-feature lint run.

`aya-backend` was **skipped entirely on this host** — not verifiable on win32: aya-obj requires std::os::fd for the tests, and cross-linting needs a Linux C toolchain for zstd-sys. Reported as skipped rather than as a pass or a failure, because neither would be true. CI runs on Linux natively and checks it there.

### By area

Decomposed so the total is checkable rather than taken on trust. Counts are from the default feature set.

| Area | Tests |
|---|---|
| `installscope-core/rules` | 42 |
| `installscope-lockfile` | 40 |
| `installscope-recorder` | 34 |
| `installscope-report` | 25 |
| `installscope-recorder/parity` | 24 |
| `installscope-registry/behavior` | 22 |
| `installscope-recorder/translate` | 21 |
| `installscope-report/diff` | 21 |
| `installscope-core` | 20 |
| `installscope-core/catalog` | 20 |
| `installscope-core/findings` | 20 |
| `installscope-lockfile/pnpm` | 19 |
| `installscope-registry` | 19 |
| `installscope-registry/store` | 18 |
| `installscope-lockfile/diff` | 16 |
| `installscope-recorder/merge` | 16 |
| `installscope` | 15 |
| `installscope-lockfile/npm` | 15 |
| `installscope-report/html` | 15 |
| `installscope-report/markdown` | 15 |
| `installscope-registry/diff` | 14 |
| `installscope-report/sarif` | 14 |
| `installscope-registry/index` | 12 |
| `installscope-core/zones` | 11 |
| `installscope-abi` | 10 |
| `installscope-core/events` | 10 |
| `installscope-lockfile/model` | 10 |
| `installscope-recorder/decode` | 10 |
| `installscope-recorder/fdtable` | 10 |
| `installscope-core/coverage` | 7 |
| `installscope-recorder/session` | 7 |
| `installscope-recorder/clock` | 3 |

## Other suites

| Suite | Result | Notes |
|---|---|---|
| `harness/g2/test-parse.mjs` | 61/61 passed | Phase 0 gate tooling; golden tests over a labelled synthetic strace fixture. Separate from cargo. |
| `harness/corpus/test-corpus.mjs` | 60/60 passed | Phase 5 corpus harness; asserts what the scripts refuse to say, not just that they run. Separate from cargo. |
| `cargo fmt --check` | clean | Rules.md §6 requires this. |

## Reproducing

```sh
# CI (Linux, native). Checks both feature sets.
node scripts/test-log.mjs

# Windows dev machine. msvc has no linker here, so the gnu toolchain is used; the
# aya-backend set is skipped entirely (see the note above).
INSTALLSCOPE_CARGO_TOOLCHAIN=stable-x86_64-pc-windows-gnu \
  node scripts/test-log.mjs
```

`INSTALLSCOPE_CARGO_TARGET` may be set to lint against another target, but note that since the registry crate acquired a C dependency, cross-linting from Windows also needs a Linux C toolchain.

Workflow runs that verified behaviour on a real kernel are recorded in `Memory.md`; this file covers only what a local suite can establish.
