# Test log

Generated by `node scripts/test-log.mjs`. **Do not edit by hand** — a count typed manually is wrong as soon as the next test lands, and a stale number here would look authoritative while being false.

- Generated: `2026-08-31T10:49:17.734Z`
- Host platform: `linux`
- Toolchain: `rustc 1.98.0 (88d9e12ae 2026-08-18)`
- Overall: **all checks passed**

**Counts are host-dependent.** `recorder/src/strace.rs` tests are `cfg(target_os = "linux")`, the E2E suite is Linux-only, and the aya-backend tests cannot link on Windows at all. A Linux run of this commit therefore reports more tests than a Windows run — more of the suite exists there. The committed copy of this file is generated on Linux, which is the platform the product runs on.

## What is NOT covered

Leading with this because a bare pass count would misrepresent the state of the project. Two areas carry real risk and no automated coverage:

| Area | Tests | Why |
|---|---|---|
| `recorder/aya-ebpf/` | **0** | Needs nightly + `bpfel-unknown-none`, which the dev machine cannot target. Never compiled. Tracepoint argument offsets are an unverified assumption. |
| `recorder/src/aya.rs` load/attach/drain | **0 executed** | Unit tests cover record decoding and zone matching; the path that loads BPF programs and drains perf buffers has never run. |
| `recorder/src/strace.rs` `record()` | 11 `#[ignore]`d | Needs Linux with `strace`. Run by `phase1-e2e.yml`, not by `cargo test`. |
| `harness/g1/` | 0 | Gate tooling. Verified by the G1 workflow run, not by unit tests. |

The first two are why `phase2-aya.yml` exists and why the first run of it is expected to fail.

## Rust tests

| Feature set | Passed | Failed | Ignored | clippy `-D warnings` |
|---|---|---|---|---|
| `default` | 139 | 0 | 10 | clean |
| `aya-backend` | 146 | 0 | 10 | clean |

Both feature sets are listed because the aya backend is optional: a warning in `recorder/src/aya.rs` is invisible to a default-feature lint run.

### By area

Decomposed so the total is checkable rather than taken on trust. Counts are from the default feature set.

| Area | Tests |
|---|---|
| `installscope-recorder` | 37 |
| `installscope-recorder/parity` | 21 |
| `installscope-recorder/translate` | 19 |
| `installscope-recorder/merge` | 14 |
| `installscope-core/events` | 10 |
| `installscope-recorder/decode` | 10 |
| `installscope-recorder/fdtable` | 10 |
| `installscope` | 9 |
| `installscope-abi` | 7 |
| `installscope-recorder/session` | 7 |
| `installscope-recorder/clock` | 3 |
| `installscope-recorder/strace` | 2 |

## Other suites

| Suite | Result | Notes |
|---|---|---|
| `harness/g2/test-parse.mjs` | 61/61 passed | Phase 0 gate tooling; golden tests over a labelled synthetic strace fixture. Separate from cargo. |
| `cargo fmt --check` | clean | Rules.md §6 requires this. |

## Reproducing

```sh
# CI (Linux, native):
node scripts/test-log.mjs

# Windows dev machine (msvc has no linker here; clippy runs against the Linux target):
INSTALLSCOPE_CARGO_TOOLCHAIN=stable-x86_64-pc-windows-gnu \
INSTALLSCOPE_CARGO_TARGET=x86_64-unknown-linux-gnu \
  node scripts/test-log.mjs
```

Workflow runs that verified behaviour on a real kernel are recorded in `Memory.md`; this file covers only what a local suite can establish.
